import { render } from "@testing-library/react";
import Appointment from "../Appointment";


describe("show specific appointment with appointment details", () => {
  it("renders properly", () => {
    render(<Appointment />);
  });
 
});
